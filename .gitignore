.DS_Store
.env

# Claude Code / agent session state — never publish
.claude/
.playwright-cli/
MEMORY/

# Tooling artifacts — this site has no build step
node_modules/
package.json
bun.lock

# Holds the Apps Script endpoint + shared secret
scripts/.ingest-config.json
